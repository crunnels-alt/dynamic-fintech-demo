{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "npm install --omit=dev"
      ]
    }
  },
  "deploy": {
    "startCommand": "npm run start:production"
  }
}
