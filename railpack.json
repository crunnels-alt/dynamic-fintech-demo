{
  "$schema": "https://schema.railpack.com",
  "_comment": "Updated to use npm install instead of npm ci",
  "steps": {
    "install": {
      "commands": [
        "npm install --omit=dev"
      ]
    }
  },
  "deploy": {
    "startCommand": "npm run start:production"
  }
}
